# Round 8 — superseded before running

This script (pairing the real, unmodified `layout1.xml` with a simplified data.xml written in our
own style) was drafted as the next bisection step after round 7's failure, but never run: the
maintainer asked whether Microsoft or a third party publish a validator with precise schema-error
detail, which led straight to round 9 (the Open XML SDK's `OpenXmlValidator`) and the actual root
cause in a single pass. Kept for the record, not executed.
